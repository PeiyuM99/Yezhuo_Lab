# Node / JS logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Diagnostic reports
report.[0-9]*.[0-9]*.[0-9]*.[0-9]*.json

# Runtime data / PIDs
pids
*.pid
*.seed
*.pid.lock

# Coverage / test output
lib-cov
coverage
*.lcov
.nyc_output

# Build / bundler / caches
.cache
.parcel-cache
.vite
.vite/cache
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Dependency directories
node_modules/
jspm_packages/

# Optional npm cache dir
.npm

# Lock files (keep these in repo generally - do NOT ignore package-lock.json unless you have a reason)
# package-lock.json
# yarn.lock

# Environment files
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# IDE / Editor
.vscode/
.idea/
*.sublime-workspace
*.sublime-project

# OS files
.DS_Store
Thumbs.db

# Build outputs
dist
out
.next
.nuxt
.vuepress/dist
.vitepress/dist

# Misc
.tgz
*.tgz

# Tooling / CI / frameworks
.next
.serverless/
.firebase/
.dynamodb/

# Vercel local config
.vercel/

# Other common caches and temp
.cache/
.tmp/
.temp/

# Yarn v3 (if used)
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Vite logs (already above but keep for redundancy)
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
